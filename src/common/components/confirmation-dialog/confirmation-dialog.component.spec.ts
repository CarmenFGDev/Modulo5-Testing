import * as React from 'react';
import { ConfirmationDialogComponent, Props } from './confirmation-dialog.component';
import { render } from '@testing-library/react';

describe('common/ConfirmationDialogComponent', () => {
  it('should be render as expected passing required properties', () => {
    // Arrange
    const props : Props= {
        isOpen: true,
        onAccept: jest.fn(),
        onClose: jest.fn(),
        title: 'Confirmation',
        labels: {
        closeButton: 'Close',
        acceptButton: 'Accept'
        },
        children: null
    };
   const result= render(<ConfirmationDialogComponent {...props}>test children</ConfirmationDialogComponent>);
   
  });
});


































































































































































































































































































































































































































































































































































































































































































